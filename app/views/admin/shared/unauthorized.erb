<h1 class="text-danger"><i class="fa fa-hand-paper-o"></i> Unauthorized</h1>

<h3 class="text-danger">Your role [ <%= current_user.role %> ] do not have permission to perform this action.</h3>

<div class="row">
  <div class="col-md-3">
    <table class="table table-bordered table-condensed summary">
      <tr>
        <td>User:</td>
        <td><%= current_user.name_with_id %></td>
      </tr>
      <tr>
        <td>Resource:</td>
        <td><%= controller_name %></td>
      </tr>
      <tr>
        <td>Action:</td>
        <td><%= action_name %></td>
      </tr>
      <tr>
        <td>ID:</td>
        <td><%= params[:id]%></td>
      </tr>
    </table>
  </div>
</div>

<br>

<h4 class="text-danger">
  <i class="fa fa-info-circle"></i>&nbsp;
<% if current_user.role.has_permission?(:role, :update) %>
  <%= link_to "Click here to update permissions", admin_system_role_path(current_user.role) %>
<% else %>
  Please contact an administrator to gain access to this resource/action.
<% end %>
</h4>

<% content_for :breadcrumbs do %>
  Access Denied
<% end %>
